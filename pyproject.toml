[project]
name = "studyassistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shash-v", email = "mw.kryptix@gmail.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
studyassistant = "studyassistant:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
