{
  "name": "my-study-hud-root",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "uv run --directory backend uvicorn app.main:app --port 8000",
    "dev:tauri": "npm --prefix frontend run tauri dev",
    "dev": "concurrently \"npm run dev:tauri\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}